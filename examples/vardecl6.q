Q main(int arg) {
    if (arg > 10)
        int x = foo(arg, arg . arg);
    else
        int x = foo(arg, arg + arg);
    int x = arg + 2;
    return arg + 1;
}

int foo(Q n, Q k) {
    return (int)n * (int)left((Ref)k);
}
